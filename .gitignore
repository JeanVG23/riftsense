# Secrets
.env
.env.*
!.env.example

# Config locale des comptes suivis (copier accounts.example.json)
config/accounts.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Données / dataset (ne pas versionner les games brutes)
data/
poc/output/
*.parquet
*.duckdb
*.rofl
captures/
frames/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Agent local config (Claude Code)
.claude/

# Scratch state for subagent-driven-development (task briefs, review packages, ledger)
.superpowers/

# Cloudflare Worker local state
web/cf/node_modules/
web/cf/.dev.vars
web/cf/.wrangler/
.demo/

# Documentation de conception (specs, plans) : hors git, hors CI.
# `docs/*` et non `docs/` : ignorer le dossier lui-meme rendrait la
# negation ci-dessous inoperante (git n'ouvre pas un repertoire exclu).
docs/*
!docs/MODEL_CARD.md
info_gcp.md
